# Global agent instructions

Canonical file: `~/.agents/AGENTS.md`. Symlinked to `~/.claude/CLAUDE.md` and
`~/.codex/AGENTS.md`, so every tool reads this one file. Edit it here.

## Conventions

- Handoff documents go in `~/handoffs/<slug>.md`. Not the workspace, not `$TMPDIR`.
